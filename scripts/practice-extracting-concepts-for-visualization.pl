#!/usr/bin/perl -w

# load a sample file and process

# we want a comprehensive markup system

# interrelate with various systems - specifically the named entity
# database system (btw, store the images for furture use with the same
# named entities), as well as formalize, markup, text::analysis,
# workhorse, etc.
